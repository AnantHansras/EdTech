name: "Validation Agent"
description: "Validates each remediation plan entry"

runs:
  using: "composite"
  steps:
    - name: Download remediation plan
      uses: actions/download-artifact@v4
      with:
        name: remediation-plan
        path: .
