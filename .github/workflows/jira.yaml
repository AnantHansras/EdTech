name: Create Jira Ticket

# Trigger manually from the Actions tab, or on push to main — adjust as needed
on:
  workflow_dispatch:
  # push:
  #   branches: [ main ]

jobs:
  create-jira-ticket:
    runs-on: ubuntu-latest
    steps:
# - name: Login to Jira
#   uses: atlassian/gajira-login@v3
#   env:
#     JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
#     JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
#     JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}

# - name: Create Jira issue
#   uses: atlassian/gajira-create@v3
#   with:
#     project: ${{ secrets.JIRA_PROJECT_KEY }}
#     issuetype: Task
#     summary: "Dummy ticket summary - automated test"
#     description: "This is a dummy description created automatically via GitHub Actions."




# - name: Install jira-cli
#   run: |
#     curl -LO https://github.com/ankitpokhrel/jira-cli/releases/latest/download/jira_linux_amd64.tar.gz
#     tar -xzf jira_linux_amd64.tar.gz
#     sudo mv jira_linux_amd64/bin/jira /usr/local/bin/

# - name: Create ticket
#   env:
#     JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
#   run: |
#     jira init --server ${{ secrets.JIRA_BASE_URL }} --login <email>
#     jira issue create -p ${{ secrets.JIRA_PROJECT_KEY }} -t Task \
#       -s "Dummy ticket summary" -b "Dummy description"




# - name: Set up Python
#   uses: actions/setup-python@v5
#   with:
#     python-version: '3.x'

# - name: Create Jira ticket
#   env:
#     JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
#     JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
#     JIRA_PROJECT_KEY: ${{ secrets.JIRA_PROJECT_KEY }}
#   run: |
#     pip install jira
#     python3 -c "
#     from jira import JIRA
#     import os
#     jira = JIRA(server=os.environ['JIRA_BASE_URL'], token_auth=os.environ['JIRA_API_TOKEN'])
#     issue = jira.create_issue(
#         project=os.environ['JIRA_PROJECT_KEY'],
#         summary='Dummy ticket summary',
#         description='Dummy description',
#         issuetype={'name': 'Task'}
#     )
#     print(f'Created: {issue.key}')
#     "
